---
description: Inspiration 项目开发规范 - Electron + React + TypeScript 桌面应用
alwaysApply: true
---

# Inspiration 项目规范

基于 README.md 的项目规范，适用于本项目的开发。

## 技术栈

- **桌面框架**: Electron ^39.2.6
- **前端框架**: React ^19.2.1
- **构建工具**: electron-vite ^5.0.0
- **语言**: TypeScript ^5.9.3
- **包管理器**: pnpm

### 前端生态

- UI: antd ^6.2.2 + @ant-design/icons ^6.1.0
- 拖拽: @dnd-kit/core ^6.3.1, @dnd-kit/sortable ^10.0.0
- 通信: @trpc/server/client ^11.9.0

### 后端/数据层

- ORM: drizzle-orm ^0.45.1
- 数据库: better-sqlite3 ^12.6.2
- 工具: drizzle-kit ^0.31.8
- 校验: zod ^3.24.1 + drizzle-zod ^0.8.3

### 多媒体处理

- ffmpeg-static ^5.3.0
- ffprobe-static ^3.1.0
- fluent-ffmpeg ^2.1.3

## 目录结构

```
src/
├── main/              # Electron 主进程
│   ├── index.ts       # 应用入口，窗口管理
│   ├── logger.ts      # 日志初始化
│   ├── router.ts      # tRPC 主路由聚合
│   ├── trpc.ts        # tRPC IPC 处理器
│   ├── db/            # 数据库
│   └── routers/       # 业务路由
├── preload/           # 预加载脚本
├── renderer/          # React 前端
│   └── src/
│       ├── components/  # UI 组件（TitleBar, SnifferPanel 等）
│       ├── pages/        # 页面组件
│       ├── lib/          # 工具库（trpc.ts）
│       └── app/          # 应用级目录
└── shared/            # 共享代码
    ├── db/            # 数据库 Schema
    └── routers/       # tRPC 路由定义
```

## 代码规范

### 文件命名

- 组件目录: `PascalCase`（如 `TitleBar/`）
- 组件文件: `index.tsx`（目录即组件）
- 其他文件: `camelCase`

### 组件结构

组件代码直接写在 `index.tsx` 中，不拆分单独文件。复杂逻辑提取到 `app/hooks/`。

### Prettier 配置

```yaml
singleQuote: true
semi: false
printWidth: 120
trailingComma: none
```

### ESLint 规则

- 关闭: `@typescript-eslint/no-explicit-any`、`explicit-function-return-type`
- 警告: `@typescript-eslint/no-unused-vars`、`prefer-const`

## 数据库 Schema

使用 `drizzle-orm` + `drizzle-zod`:

- Schema 放在 `src/shared/db/`
- 基础字段（id, createdAt, updatedAt）在 `base.ts` 中管理
- 使用 `$inferSelect`（查询）、`$inferInsert`（插入）

## tRPC 通信

### 后端定义

```typescript
export const bookmarkRouter = router({
  list: publicProcedure.query(async () => { ... }),
  create: publicProcedure
    .input(bookmarkCreateSchema)
    .mutation(async ({ input }) => { ... }),
})
```

### 前端调用

```typescript
const bookmarks = await trpc.bookmark.list.query()
await trpc.bookmark.create.mutate({ name: 'test', type: 2 })
```

## 三层媒体嗅探架构

- **Layer 1**: DOM 扫描（`<img>`, `<video>`, `<audio>` 等标签）
- **Layer 2**: 响应头分析（`onResponseStarted` + Content-Type）
- **Layer 3**: ffprobe 探测 + URL 启发式分析

## UI 风格

基于 Ant Design 6.x 紧凑主题:

```typescript
const antdTheme = {
  token: {
    colorPrimary: '#1677ff',
    fontSize: 12,
    borderRadius: 4,
    controlHeight: 28,
  },
}
```

## 常用命令

```bash
pnpm dev          # 开发模式
pnpm build        # 构建
pnpm db:run       # 数据库迁移
pnpm db:push      # 推送 schema
pnpm db:studio    # GUI 管理
pnpm build:win    # Windows 构建
```

## 日志使用

```typescript
import log from './logger'

log.info('应用启动')
log.debug('调试信息', { data })
log.error('错误', error)
```
